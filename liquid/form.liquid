


      \
          
          {%- if form.errors contains 'email' -%}
            <small class="contact__field-error" id="ContactForm-email-error">
              <span class="visually-hidden">{{ 'accessibility.error' | t }}</span>
              <span class="form__message">
                <span class="svg-wrapper">
                  {{- 'icon-error.svg' | inline_asset_content -}}
                </span>
                {{- form.errors.translated_fields.email | capitalize }}
                {{ form.errors.messages.email -}}
              </span>
            </small>
          {%- endif -%}
        </div>
      </div>
      <div class="field">
        <input
          type="text"
          id="ContactForm-company"
          class="field__input"
          autocomplete="organization"
          name="contact[company]"
          placeholder="Company / Organization"
        >
        <label class="field__label" for="ContactForm-company">Company / Organization</label>
      </div>
      <div class="field">
        <input
          type="tel"
          id="ContactForm-phone"
          class="field__input"
          autocomplete="tel"
          name="contact[{{ 'templates.contact.form.phone' | t }}]"
          pattern="[0-9\-]*"
          value="{% if form.phone %}{{ form.phone }}{% elsif customer %}{{ customer.phone }}{% endif %}"
          placeholder="{{ 'templates.contact.form.phone' | t }}"
        >
        <label class="field__label" for="ContactForm-phone">{{ 'templates.contact.form.phone' | t }} (optional)</label>
      </div>
      <div class="field">
        <textarea
          rows="10"
          id="ContactForm-body"
          class="text-area field__input"
          name="contact[project_details]"
          placeholder="Tell us about the product(s), colors, imprint method, event date, and anything else we should know."
        >
          {{- form.body -}}
        </textarea>
        <label class="form__label field__label" for="ContactForm-body">
          Project details
        </label>
      </div>
      <div class="contact__fields">
        <div class="field">
          <select id="ContactForm-quantity" class="field__input" name="contact[quantity]">
            <option value="">Select</option>
            <option value="1-10">1-10</option>
            <option value="10-50">10-50</option>
            <option value="50-100">50-100</option>
            <option value="100+">100+</option>
          </select>
          <label class="field__label" for="ContactForm-quantity">Quantity</label>
        </div>
        <div class="field">
          <select id="ContactForm-budget" class="field__input" name="contact[budget]">
            <option value="">Select</option>
            <option value="< $500">< $500</option>
            <option value="$500 - $1000">$500 - $1000</option>
            <option value="$1000 - $5000">$1000 - $5000</option>
            <option value="$5000+">$5000+</option>
          </select>
          <label class="field__label" for="ContactForm-budget">Budget</label>
        </div>
        <div class="field">
          <select id="ContactForm-timeline" class="field__input" name="contact[timeline]">
            <option value="">Select</option>
            <option value="1-2 Weeks">1-2 Weeks</option>
            <option value="2-4 Weeks">2-4 Weeks</option>
            <option value="4+ Weeks">4+ Weeks</option>
          </select>
          <label class="field__label" for="ContactForm-timeline">Timeline</label>
        </div>
      </div>
      <div class="field">
        <label class="field__label" for="ContactForm-artwork">Attach artwork (optional)</label>
        <input type="file" id="ContactForm-artwork" class="field__input" name="contact[artwork]">
      </div>
      <div class="contact__button">
        <button type="submit" class="button">
          {{ 'templates.contact.form.send' | t }}
        </button>
      </div>
        {%- endform -%}
      </div>
      <div class="contact-info">
          <h3>Talk to a real person</h3>
          <p>Email<br><a href="mailto:sales@makemypromo.com">sales@makemypromo.com</a></p>
          <p>Phone<br><a href="tel:7044912889">(704) 491-2889</a></p>
          <p>Hours<br>Mon-Fri, 9:00am-6:00pm ET</p>
          <p>Office<br>520 - B Union West Blvd., Matthews, NC 28104</p>
          <div class="social-links">
            <a href="#"><svg><!-- instagram icon --></svg></a>
            <a href="#"><svg><!-- facebook icon --></svg></a>
            <a href="#"><svg><!-- linkedin icon --></svg></a>
          </div>
          <p>Average first response time: <strong>under 4 business hours.</strong></p>

          <h3>Quick FAQs</h3>
          <div class="faq">
            <details>
              <summary>How fast can you deliver?</summary>
              <p>Delivery times vary depending on the product and customization. Please contact us for a specific quote.</p>
            </details>
            <details>
              <summary>Can you match my brand colors?</summary>
              <p>Yes, we can match Pantone colors for most products. Please provide your Pantone color codes when you place your order.</p>
            </details>
            <details>
              <summary>What file types do you accept?</summary>
              <p>We accept AI, EPS, and PDF files. We can also work with PNG and JPG files, but vector files are preferred for the best quality.</p>
            </details>
          </div>
        </div>
    </div>
  </div>
</div>
<style>
  .contact-container {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: 5rem;
  }
  .contact-form h2 {
    font-size: 2.5rem;
    font-weight: bold;
    margin-bottom: 0.5rem;
  }
  .contact-form p {
    margin-bottom: 2rem;
    color: #6c757d;
  }
  .contact-info {
    background-color: #f8f9fa;
    padding: 2rem;
    border-radius: 8px;
  }
  .contact-info h3 {
    font-size: 1.5rem;
    font-weight: bold;
    margin-bottom: 1.5rem;
  }
  .contact-info p {
    margin-bottom: 1.5rem;
    line-height: 1.6;
  }
  .contact-info a {
    color: #007bff;
    text-decoration: none;
  }
  .contact-info a:hover {
    text-decoration: underline;
  }
  .social-links {
    margin-bottom: 1.5rem;
  }
  .social-links a {
    display: inline-block;
    margin-right: 1rem;
    border: 1px solid #ced4da;
    border-radius: 50%;
    width: 40px;
    height: 40px;
    text-align: center;
    line-height: 40px;
  }
  .faq details {
    border: 1px solid #e9ecef;
    border-radius: 8px;
    padding: 1rem;
    margin-bottom: 1rem;
  }
  .faq summary {
    font-weight: bold;
    cursor: pointer;
  }
  .field__input, .text-area {
    border-radius: 8px !important;
    border: 1px solid #ced4da !important;
  }
  .contact__button .button {
    width: 100%;
    padding: 1rem;
    font-size: 1rem;
    background-color: #007bff;
    border: none;
    border-radius: 8px;
  }
  @media screen and (max-width: 990px) {
    .contact-container {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
  }
</style>

{% schema %}
{
  "name": "t:sections.contact-form.name",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "inline_richtext",
      "id": "heading",
      "default": "t:sections.contact-form.settings.title.default",
      "label": "t:sections.contact-form.settings.title.label"
    },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        {
          "value": "h2",
          "label": "t:sections.all.heading_size.options__1.label"
        },
        {
          "value": "h1",
          "label": "t:sections.all.heading_size.options__2.label"
        },
        {
          "value": "h0",
          "label": "t:sections.all.heading_size.options__3.label"
        },
        {
          "value": "hxl",
          "label": "t:sections.all.heading_size.options__4.label"
        },
        {
          "value": "hxxl",
          "label": "t:sections.all.heading_size.options__5.label"
        }
      ],
      "default": "h1",
      "label": "t:sections.all.heading_size.label"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ],
  "presets": [
    {
      "name": "t:sections.contact-form.presets.name"
    }
  ]
}
{% endschema %}
